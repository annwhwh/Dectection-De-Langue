package corpus;

import java.io.File;
import java.nio.file.Path;

public class Propriete {

    static public final String PATH_RESOURCE = "res" + File.separator;
	static public final String PATH_ALPHABET = PATH_RESOURCE + "alphabet" + File.separator;
	static public final String PATH_CORPUS = PATH_RESOURCE + "corpus" + File.separator;
	static public final String PATH_PROFIL = PATH_RESOURCE + "language_profil" + File.separator;
	public static final String PATH_TEXT = PATH_RESOURCE + File.separator + "test_text" + File.separator;
}
